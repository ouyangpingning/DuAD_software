-r requirements.txt

# NVIDIA GPU 推理（Linux x86_64 + CUDA 13 + TensorRT，当前开发机配置）
onnxruntime-gpu==1.28.0
# TensorRT 只装 *_libs 包：它提供 ORT 1.28 TensorrtExecutionProvider 所需的
# libnvinfer.so.10（装入 site-packages/tensorrt_libs/，activate 已注入 LD_LIBRARY_PATH）。
# 不要装 tensorrt / tensorrt_cu13 绑定包：11.2.1.2 绑定会依赖 TRT 11 的
# tensorrt_cu13_libs==11.2.1.2（libnvinfer.so.11），与 ORT 1.28 的 TRT 10 EP 冲突。
tensorrt_cu13_libs==10.16.1.11
